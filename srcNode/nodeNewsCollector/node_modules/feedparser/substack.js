var feedparser = require('./')
  , request = require('request')
  , fs = require('fs');

var feed = 'http://substack.net/blog.xml';

request.get(feed).pipe(feedparser({ feedurl: feed }))
  .on('complete', function (meta, articles) {
    console.log(meta);
    console.log(articles[0]);
  });